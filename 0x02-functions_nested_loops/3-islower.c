#include "main.h"

/**
* _islower - checks for lowercase character
* @c: character to print
*
* Return: lowercase 1 otherwise 0
*/
int _islower(int c)
{
	int i;

	i = islower(c);
	if (i = True)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
