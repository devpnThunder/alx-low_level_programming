#include "main.h"

/**
* _abs - computes absolute value of an integer
* @i: absolute value
*
* Return: if successful 0
*/
int _abs(int i)
{
	if (i >= 0)
	{
		return (1);
	}
	else
	{
		return (i * -1);
	}
}
